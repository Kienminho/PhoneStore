<h4 class="mb-2">Chào mừng đến Sneat Mobile! 👋</h4>
<p class="mb-4">Đổi mật khẩu</p>

<form id="formAuthentication" class="mb-3" action="index.html" method="POST">
    <div class="mb-3">
        <label for="new-password" class="form-label">Mật khẩu mới</label>
        <input type="password" class="form-control" id="new-password" name="new-password"
            placeholder="Nhập mật khẩu mới" autofocus aria-describedby="password" />
    </div>
    <div class="mb-3 form-password-toggle">
        <div class="d-flex justify-content-between">
            <label class="form-label" for="password">Xác nhận mật khẩu</label>
        </div>
        <div class="input-group input-group-merge">
            <input type="password" id="confirm-password" class="form-control" name="confirm-password"
                placeholder="Xác nhận lại mật khẩu" aria-describedby="password" />

        </div>
    </div>
    <p class="text-left ">
        <span class="message d-none text-danger"></span>
        <span class="message-success d-none text-success"></span>
    </p>
    <div class="mb-3">
        <button class="btn btn-primary d-grid w-100" type="button">Xác nhận</button>
    </div>
</form>

<p class="text-center">
    <span>Đổi mật khẩu để tiếp tục?</span>
    <a href="auth-register-basic.html">
        <span>Đăng nhập</span>
    </a>
</p>

<script src="/js/changePassword.js"></script>